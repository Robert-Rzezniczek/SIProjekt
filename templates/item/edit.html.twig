{% include 'default/_form.html.twig' with {
    page_title: 'title.item_edit'|trans({'%id%': item.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'item_index',
} %}