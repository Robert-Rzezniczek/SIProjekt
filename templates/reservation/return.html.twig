{% include 'default/_form.html.twig' with {
    page_title: 'title.request_return'|trans({'%id%': item.id|default('')}),
    form: form,
    submit_label: 'action.request_return'|trans,
    back_to_list_path: 'reservation_my',
} %}